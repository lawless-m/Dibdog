id = "0003-delete-empty-match"
status = "meaningful"
provenance = "manual"
tags = ["delete", "dml", "string-literal", "safe-mutation"]
notes = """
DELETE with a WHERE that matches zero rows on the live `product` table
(verified: `code = 'NOPE'` is not a real product code on rivsem04).
The engine accepts the statement and reports a successful execution;
the table is not mutated. Sibling of 0002-update-empty-match — same
predicate shape, just with DELETE instead of UPDATE, to exercise the
DELETE statement path through the grammar.
"""
fixtures = []

[[history]]
at = "2026-05-27T13:36:29Z"
from = "pending"
to = "meaningful"
by = "claude-code"
reason = "Promotion checks passed during slice #48 — DELETE statement."
