SELECT PRODUCT.CODE, PRODUCT.DESC1, PRODUCT."GROUP", MAIN.DESC, SUB.DESC, SUBSUB.DESC FROM NISAINT_CS\PRODUCT PRODUCT LEFT JOIN PRODGRP MAIN ON LEFT(GROUP,2) = MAIN.CODE LEFT JOIN PRODGRP SUB ON LEFT(GROUP,4) = SUB.CODE LEFT JOIN PRODGRP SUBSUB ON GROUP = SUBSUB.CODE
